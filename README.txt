1. Ensure that the blacklist file is in the same directory as "main.py"
2. Use the command "python3 main.py <port> <flag_telemetry> <filename of blacklist>" to run the proxy
<port> is port number defined by the user for use in connecting to the proxy.
<flag_telemetry> is either 1 or 0 depending if telemetry is desired.
